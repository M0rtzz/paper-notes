name: Search Engine Indexing Submit (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Indexing disabled
        run: echo "Search engine indexing is disabled for this fork."
